# 微信公众号RSS新闻收集系统需求文档

## 1. 项目概述

### 1.1 项目名称
RSSInfomation - 基于微信公众号的RSS科技/AI新闻收集和展示系统

### 1.2 项目背景
- 利用微信公众号个人账号的自动回复功能
- 实现科技/AI新闻的自动收集和定时推送
- 为用户提供便捷的新闻获取渠道

### 1.3 技术栈
- **后端语言**: Python 3.8+
- **Web框架**: Flask
- **数据库**: MySQL 8.0+
- **ORM**: SQLAlchemy
- **定时任务**: APScheduler
- **RSS解析**: feedparser
- **HTTP客户端**: requests
- **配置管理**: python-dotenv
- **日志**: logging

## 2. 功能需求

### 2.1 RSS新闻收集功能
- **定时收集**: 每天早上5点自动执行RSS数据收集
- **RSS源管理**: 支持多个科技/AI相关的RSS源
- **数据解析**: 解析RSS内容，提取标题、摘要、链接、发布时间等信息
- **去重机制**: 避免重复新闻数据
- **数据存储**: 将解析后的新闻数据存储到MySQL数据库

### 2.2 数据管理功能
- **数据生命周期**: 仅保留最近7天的新闻数据
- **自动清理**: 超过7天的数据自动删除，为新数据腾出空间
- **数据分类**: 支持新闻分类管理（科技、AI等）
- **数据统计**: 提供新闻数量统计功能

### 2.3 微信公众号接口功能
- **自动回复**: 通过微信公众号自动回复接口展示新闻
- **今日新闻**: 仅展示当天的新闻数据
- **格式化输出**: 新闻内容格式化为适合微信展示的格式
- **错误处理**: 处理无新闻数据时的友好提示

### 2.4 配置管理功能
- **配置文件**: 通过配置文件管理微信API密钥、数据库连接等敏感信息
- **环境变量**: 支持通过环境变量覆盖配置文件设置
- **RSS源配置**: 可配置的RSS源列表，便于扩展

## 3. 系统架构设计

### 3.1 整体架构
```
┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│  微信公众号API   │────│   Flask Web服务  │────│   MySQL数据库    │
└─────────────────┘    └─────────────────┘    └─────────────────┘
                              │
                              │
                    ┌─────────────────┐
                    │  RSS定时收集器   │
                    └─────────────────┘
```

### 3.2 模块划分

#### 3.2.1 核心模块 (core/)
- **app.py**: Flask应用主入口
- **config.py**: 配置管理模块
- **database.py**: 数据库连接和初始化
- **exceptions.py**: 自定义异常类

#### 3.2.2 RSS收集模块 (rss/)
- **collector.py**: RSS收集器主类
- **parser.py**: RSS解析器接口和实现
- **scheduler.py**: 定时任务调度器
- **sources.py**: RSS源管理

#### 3.2.3 数据模型模块 (models/)
- **news.py**: 新闻数据模型
- **source.py**: RSS源数据模型
- **base.py**: 基础模型类

#### 3.2.4 微信接口模块 (wechat/)
- **api.py**: 微信公众号API接口
- **message_handler.py**: 消息处理器
- **formatter.py**: 新闻内容格式化器

#### 3.2.5 服务模块 (services/)
- **news_service.py**: 新闻业务逻辑服务
- **wechat_service.py**: 微信业务逻辑服务
- **rss_service.py**: RSS业务逻辑服务

#### 3.2.6 工具模块 (utils/)
- **logger.py**: 日志工具
- **helpers.py**: 辅助函数
- **validators.py**: 数据验证工具

### 3.3 设计原则
- **高内聚**: 每个模块专注于单一职责
- **低耦合**: 模块间通过接口通信，减少依赖
- **可扩展**: RSS解析器采用策略模式，便于添加新的RSS源
- **可配置**: 所有配置项通过配置文件管理
- **可测试**: 模块化设计便于单元测试

## 4. 数据库设计

### 4.1 新闻表 (news)
```sql
CREATE TABLE news (
    id INT AUTO_INCREMENT PRIMARY KEY,
    title VARCHAR(500) NOT NULL COMMENT '新闻标题',
    summary TEXT COMMENT '新闻摘要',
    content TEXT COMMENT '新闻内容',
    link VARCHAR(1000) NOT NULL COMMENT '新闻链接',
    author VARCHAR(100) COMMENT '作者',
    category VARCHAR(50) NOT NULL COMMENT '新闻分类',
    source_name VARCHAR(100) NOT NULL COMMENT 'RSS源名称',
    source_url VARCHAR(500) NOT NULL COMMENT 'RSS源URL',
    pub_date DATETIME NOT NULL COMMENT '发布时间',
    collect_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '收集时间',
    guid VARCHAR(500) COMMENT 'RSS唯一标识',
    hash_code VARCHAR(64) UNIQUE NOT NULL COMMENT '内容哈希值，用于去重',
    INDEX idx_category (category),
    INDEX idx_pub_date (pub_date),
    INDEX idx_collect_time (collect_time),
    INDEX idx_hash_code (hash_code)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='新闻数据表';
```

### 4.2 RSS源表 (rss_sources)
```sql
CREATE TABLE rss_sources (
    id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(100) NOT NULL UNIQUE COMMENT 'RSS源名称',
    url VARCHAR(500) NOT NULL UNIQUE COMMENT 'RSS源URL',
    category VARCHAR(50) NOT NULL COMMENT '分类',
    description TEXT COMMENT '描述',
    is_active BOOLEAN DEFAULT TRUE COMMENT '是否启用',
    fetch_interval INT DEFAULT 3600 COMMENT '抓取间隔(秒)',
    last_fetch_time DATETIME COMMENT '最后抓取时间',
    created_time DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    updated_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    INDEX idx_category (category),
    INDEX idx_is_active (is_active)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='RSS源配置表';
```

### 4.3 系统配置表 (system_config)
```sql
CREATE TABLE system_config (
    id INT AUTO_INCREMENT PRIMARY KEY,
    config_key VARCHAR(100) NOT NULL UNIQUE COMMENT '配置键',
    config_value TEXT COMMENT '配置值',
    description TEXT COMMENT '配置描述',
    created_time DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    updated_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    INDEX idx_config_key (config_key)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='系统配置表';
```

## 5. RSS源配置

### 5.1 推荐RSS源
- **科技类**:
  - 36氪: https://36kr.com/feed
  - 虎嗅网: https://www.huxiu.com/rss/0.xml
  - 钛媒体: https://www.tmtpost.com/feed
  - 极客公园: https://www.geekpark.net/rss

- **AI类**:
  - 机器之心: https://www.jiqizhixin.com/rss
  - 量子位: https://www.qbitai.com/feed
  - AI科技大本营: https://blog.csdn.net/rss/list/ai
  - OpenAI Blog: https://openai.com/blog/rss.xml

### 5.2 RSS源管理
- 支持动态添加/删除RSS源
- 支持启用/禁用特定RSS源
- 支持自定义抓取频率
- 支持RSS源分类管理

## 6. API接口设计

### 6.1 微信公众号接口
```
POST /wechat/message
Content-Type: application/json

Request Body:
{
    "ToUserName": "toUser",
    "FromUserName": "fromUser",
    "CreateTime": 1348831860,
    "MsgType": "text",
    "Content": "新闻",
    "MsgId": 1234567890123456
}

Response Body:
{
    "ToUserName": "fromUser",
    "FromUserName": "toUser",
    "CreateTime": 1348831860,
    "MsgType": "text",
    "Content": "【今日科技新闻】\n1. 标题1...\n2. 标题2..."
}
```

### 6.2 管理接口 (可选)
```
GET /api/news?date=2024-01-01&category=tech
POST /api/sources
PUT /api/sources/{id}
DELETE /api/sources/{id}
```

## 7. 部署和配置

### 7.1 环境要求
- Python 3.8+
- MySQL 8.0+
- Redis (可选，用于缓存)

### 7.2 配置文件 (.env)
```
# 数据库配置
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=password
DB_NAME=rss_news

# 微信公众号配置
WECHAT_APP_ID=your_app_id
WECHAT_APP_SECRET=your_app_secret
WECHAT_TOKEN=your_token
WECHAT_ENCODING_AES_KEY=your_encoding_aes_key

# 系统配置
RSS_COLLECT_HOUR=5
NEWS_RETENTION_DAYS=7
LOG_LEVEL=INFO
```

### 7.3 启动方式
```bash
# 安装依赖
pip install -r requirements.txt

# 初始化数据库
python manage.py init-db

# 启动Web服务
python app.py

# 启动RSS收集器 (生产环境建议使用supervisor)
python scheduler.py
```

## 8. 扩展性设计

### 8.1 RSS解析器扩展
采用策略模式设计RSS解析器，支持不同格式的RSS源：

```python
class RSSParser(ABC):
    @abstractmethod
    def parse(self, rss_content: str) -> List[NewsItem]:
        pass

class StandardRSSParser(RSSParser):
    def parse(self, rss_content: str) -> List[NewsItem]:
        # 标准RSS解析逻辑
        pass

class AtomRSSParser(RSSParser):
    def parse(self, rss_content: str) -> List[NewsItem]:
        # Atom格式解析逻辑
        pass
```

### 8.2 消息格式化器扩展
支持不同的消息格式化器：

```python
class MessageFormatter(ABC):
    @abstractmethod
    def format(self, news_items: List[NewsItem]) -> str:
        pass

class WechatTextFormatter(MessageFormatter):
    def format(self, news_items: List[NewsItem]) -> str:
        # 微信文本格式化
        pass

class MarkdownFormatter(MessageFormatter):
    def format(self, news_items: List[NewsItem]) -> str:
        # Markdown格式化
        pass
```

## 9. 监控和日志

### 9.1 日志记录
- RSS收集过程日志
- 数据库操作日志
- 微信API调用日志
- 系统错误日志

### 9.2 监控指标
- RSS源可用性
- 数据收集成功率
- 微信API响应时间
- 数据库性能指标

## 10. 安全考虑

### 10.1 数据安全
- 敏感配置信息加密存储
- 数据库连接使用SSL
- API密钥定期轮换

### 10.2 访问控制
- 微信接口签名验证
- 管理接口权限控制
- SQL注入防护

## 11. 测试策略

### 11.1 单元测试
- RSS解析器测试
- 数据模型测试
- 业务逻辑测试

### 11.2 集成测试
- 数据库集成测试
- 微信API集成测试
- RSS收集流程测试

### 11.3 性能测试
- 大量数据存储测试
- 高并发接口测试
- 长时间运行稳定性测试

## 12. 项目目录结构

```
RSSInfomation/
├── app.py                 # Flask应用主入口
├── requirements.txt       # 依赖包列表
├── config/               # 配置文件目录
│   ├── __init__.py
│   ├── settings.py       # 应用配置
│   └── database.py       # 数据库配置
├── core/                 # 核心模块
│   ├── __init__.py
│   ├── app.py           # Flask应用工厂
│   ├── database.py      # 数据库初始化
│   └── exceptions.py    # 自定义异常
├── models/              # 数据模型
│   ├── __init__.py
│   ├── base.py          # 基础模型
│   ├── news.py          # 新闻模型
│   └── source.py        # RSS源模型
├── rss/                 # RSS收集模块
│   ├── __init__.py
│   ├── collector.py     # RSS收集器
│   ├── parser.py        # RSS解析器
│   ├── scheduler.py     # 定时任务
│   └── sources.py       # RSS源管理
├── wechat/              # 微信接口模块
│   ├── __init__.py
│   ├── api.py           # 微信API
│   ├── message_handler.py # 消息处理
│   └── formatter.py     # 格式化器
├── services/            # 业务服务
│   ├── __init__.py
│   ├── news_service.py  # 新闻服务
│   ├── wechat_service.py # 微信服务
│   └── rss_service.py   # RSS服务
├── utils/               # 工具模块
│   ├── __init__.py
│   ├── logger.py        # 日志工具
│   ├── helpers.py       # 辅助函数
│   └── validators.py    # 验证器
├── tests/               # 测试目录
│   ├── __init__.py
│   ├── test_rss.py      # RSS测试
│   ├── test_wechat.py   # 微信测试
│   └── test_models.py   # 模型测试
├── logs/                # 日志目录
├── .env                 # 环境变量配置
├── .env.example         # 环境变量示例
├── .gitignore          # Git忽略文件
├── README.md           # 项目说明
└── manage.py           # 管理脚本
```

## 13. 开发计划

### 13.1 第一阶段 (基础功能)
- 数据库设计和初始化
- 基础RSS收集功能
- 简单的微信公众号接口

### 13.2 第二阶段 (功能完善)
- RSS解析器扩展
- 消息格式化优化
- 配置管理完善

### 13.3 第三阶段 (优化部署)
- 性能优化
- 监控和日志完善
- 部署自动化

## 14. 风险评估

### 14.1 技术风险
- RSS源不稳定或格式变化
- 微信API限制和变更
- 数据库性能问题

### 14.2 应对策略
- 多RSS源备份
- API版本兼容性处理
- 数据库优化和缓存策略

---

**文档版本**: v1.0
**创建日期**: 2024-01-01
**更新日期**: 2024-01-01
**作者**: 开发团队